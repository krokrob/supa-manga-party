<div class="container">
  <div class="row">
    <div class="col-sm-12 text-center">
      <h1>Gallerie des invités</h1>
      <div class="row">
        <% @persos.each do |perso| %>
          <div class="col-sm-4 sm-padded">
            <%= link_to perso.fullname, perso_path(perso), class:"btn btn-warning" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
